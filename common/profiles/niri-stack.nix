# Niri compositor stack. Currently a no-op marker — generic wayland
# utilities (grim/slurp/wl-clipboard/etc.) moved to common/desktop.nix
# since they're useful with any wayland compositor. Niri-specific tools
# can be added here as they appear.

{ ... }:

{ }
